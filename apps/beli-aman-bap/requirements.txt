fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# greenlet 3.5.1 ships only a macOS-universal2 wheel which breaks
# Vercel's Linux build (`uv` refuses to install). Pin < 3.5 until
# the manylinux wheels catch up.
greenlet<3.5
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.26.0
python-multipart>=0.0.6
firebase-admin>=6.5.0
mangum>=0.17.0
pynacl>=1.5.0
