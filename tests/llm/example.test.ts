import { describe, it, expect } from "vitest";

// LLM tier example — replace with real provider calls.
// Skipped by `pnpm test`; runs via `pnpm test:llm`.
describe("llm tier smoke", () => {
  it("llm tier vitest config is wired up", () => {
    expect(true).toBe(true);
  });
});
